Dev container for the up-transport-vsomeip-rust workspace.

This config provides a reproducible Rust + native build tool environment for building the workspace
which includes native bindings to `vsomeip`.

Open this repository with the Remote - Containers extension to get the development container.
