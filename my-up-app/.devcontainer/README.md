Dev container for `my-up-app`.

This config uses the official Rust devcontainer image and installs native build tools
(`cmake`, `clang`, `pkg-config`, `protobuf-compiler`, `libssl-dev`) on container creation.

Open this folder with the Remote - Containers extension to get a reproducible development environment.
