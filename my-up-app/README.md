# my-up-app

Scaffold example app using `up-transport-vsomeip`.

Build (from repository root):

```bash
cd /workspaces/docker-uprotocol/my-up-app
cargo build
```

This crate depends on the local path `../up-transport-vsomeip-rust`. Adjust the path if you move the project.
